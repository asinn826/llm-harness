# Install with: pip3.11 install -r requirements.txt
# Python 3.11+ recommended. System Python 3.9 has incompatible mlx versions.
torch>=2.2
accelerate>=0.26
requests>=2.31
html2text>=2020.1
python-dotenv>=1.0
rich>=13.0
pytest>=8.0

# macOS only — install separately if pip can't resolve:
#   pip3.11 install mlx-lm
# mlx-lm pulls in its own compatible transformers version.
mlx-lm>=0.30; sys_platform == "darwin" and python_version >= "3.11"
transformers>=5.0; sys_platform != "darwin"
